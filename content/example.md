# Example post

This is a simple markdown file placed in the `content/` folder. Click the link in the "content" section of the homepage to preview this content.

## Notes

- You can write in Markdown.
- The site will render the Markdown using a small client-side renderer.

```py
def hello():
    print("hello from example.md")
```
